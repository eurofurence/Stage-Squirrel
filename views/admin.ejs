<% var title = 'Stage Squirrel - Admin Configuration'; %>
<% include ./partials/page-begin %>

<div class="page-header text-center">
    <h1><span class="glyphicon glyphicon-cog"></span> Admin Configuration</h1>
</div>
<div class="row">
    <div class="col-sm-6">
        <div class="well">
            <h3><span class="glyphicon glyphicon-globe"></span> Global</h3>
            <p>
                <form action="<%= locals.baseUrlInfo.pathname %>admin/settings" method="post">
                    <% for (setting of settings) { %>
                    <div class="form-group">
                        <label><%= setting.configuration_title %></label>
                        <input type="text" class="form-control" name="settings[<%= setting.configuration_key %>]" value="<%= setting.configuration_value %>" />
                    </div>
                    <% } %>
                    <button type="submit" class="btn btn-primary btn-lg">Save</button>
                </form>
            </p>
        </div>
    </div>
    <div class="col-sm-10">
        <table class="table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Username</th>
                    <th>Email</th>
                    <th>Role</th>
                    <th>Created</th>
                    <th>Last Login</th>
                    <th>Active</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <form action="<%= locals.baseUrlInfo.pathname %>admin/user-activation" method="post">
                    <% for (user of users) { %>
                    <tr>
                        <td><%= user.user_id %></td>
                        <td><%= user.user_name %></td>
                        <td><%= user.user_mail %></td>
                        <td><%= user.user_role %></td>
                        <td><%= new Date(user.user_created).toLocaleString() %></td>
                        <td><%= new Date(user.user_last_login).toLocaleString() %></td>
                        <td>
                            <span class="label label-<%= user.user_active ? 'success' : 'warning' %>">
                                <%= user.user_active ? '' : 'in' %>active
                            </span>
                        </td>
                        <td>
                            <button
                                type="submit"
                                class="btn btn-primary btn-xs"
                                name="<%= user.user_active ? 'de' : '' %>activation"
                                value="<%= user.user_id %>"
                                >
                                <%= user.user_active ? 'de' : '' %>activate
                            </button>
                        </td>
                    </tr>
                    <% } %>
                </form>
            </tbody>
        </table>
    </div>
</div>

<% include ./partials/page-end %>